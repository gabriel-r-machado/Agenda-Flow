// Importa os matchers extras do @testing-library/jest-dom
// Isso adiciona matchers como:
// - expect(...).toBeInTheDocument()
// - expect(...).toBeVisible()
// - expect(...).toHaveTextContent()
// - expect(...).toBeDisabled()
// E muitos outros para facilitar os testes de React

import '@testing-library/jest-dom';
